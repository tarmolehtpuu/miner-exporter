spring:
  application:
    name: miner-exporter

server:
  forward-headers-strategy: framework
  address: 127.0.0.1
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include:
          - health