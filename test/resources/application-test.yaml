spring:
  application:
    name: miner-exporter

server:
  forward-headers-strategy: framework
  address: 172.0.0.1
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include:
          - health